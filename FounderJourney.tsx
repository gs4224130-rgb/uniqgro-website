import { useState } from 'react'
import { journey } from '../../content/site'
export function FounderJourney(){const [active,setActive]=useState(0);const s=journey[active];return <div className="journey"><div className="journey-tabs" role="tablist" aria-label="Founder journey stages">{journey.map((x,i)=><button role="tab" aria-selected={active===i} className={active===i?'active':''} onClick={()=>setActive(i)} key={x.title}>{x.n} · {x.title}</button>)}</div><article className="journey-card" role="tabpanel"><div className="eyebrow">STAGE {s.n}</div><h2 className="section-title" style={{fontSize:'clamp(3rem,6vw,6rem)'}}>{s.title}</h2><p style={{fontSize:'1.35rem',maxWidth:760}}>{s.q}</p><dl><dt>UniqGro action</dt><dd>{s.action}</dd><dt>Founder responsibility</dt><dd>{s.founder}</dd><dt>Deliverables</dt><dd>{s.deliver}</dd><dt>Decision gate</dt><dd>{s.gate}</dd><dt>Example evidence</dt><dd>{s.evidence}</dd></dl></article></div>}
