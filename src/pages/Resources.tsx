import { useState } from 'react'
import { PageHero } from '../components/common/PageHero'
import { resources } from '../data/resources'
const cats=['All','Idea Validation','Customer Discovery','Brand & Positioning','Go-to-Market','Sales','Operations','AI Systems','Founder Stories','Failure Analysis']
export default function Resources(){const [cat,setCat]=useState('All');const filtered=resources.filter(r=>cat==='All'||r.category===cat);return <><PageHero eyebrow="FOUNDER RESOURCES" title="Playbooks for the work before the applause." lead="Practical resources for founders building evidence, clarity and operating discipline."/><section className="section"><div className="container"><div className="forum-tools"><select className="select" style={{maxWidth:300}} value={cat} onChange={e=>setCat(e.target.value)}>{cats.map(c=><option key={c}>{c}</option>)}</select></div><div className="resource-grid">{filtered.map(r=><article className="resource-card" key={r.title}><div className="eyebrow">{r.category}</div><h2>{r.title}</h2><p>{r.description}</p><div style={{marginTop:'auto'}}><span className="pill">{r.status==='published'?'Published':'Coming soon'}</span><p>{r.readTime} · {r.author}</p></div></article>)}</div></div></section></>}
