import { PageHero } from '../components/common/PageHero'
import { env } from '../config/env'
export default function Contact(){return <><PageHero eyebrow="CONTACT" title="Start with the problem." lead="Use the founder application for structured support. Use this page for general enquiries and verified public channels."/><section className="section"><div className="container content-grid"><div><h2 className="section-title">Contact UniqGro</h2><p><strong>Business email:</strong> {env.contactEmail || '[Configure VITE_CONTACT_EMAIL before publishing]'}</p><p><strong>WhatsApp:</strong> {env.whatsapp ? 'Configured' : '[Configure VITE_WHATSAPP_NUMBER]'}</p><p><strong>LinkedIn:</strong> {env.linkedin ? 'Configured' : '[Configure verified link]'}</p><p><strong>Instagram:</strong> {env.instagram ? 'Configured' : '[Configure verified link]'}</p><div className="notice">Do not submit confidential business secrets, passwords, financial credentials or sensitive personal information through the general contact form.</div></div><form className="card" onSubmit={e=>{e.preventDefault();alert('Contact delivery requires a configured backend or form provider.')}}><div className="field"><label>Name</label><input className="input" required/></div><div className="field"><label>Email</label><input className="input" type="email" required/></div><div className="field"><label>Message</label><textarea className="textarea" rows={6} required/></div><button className="btn primary" type="submit">Send enquiry</button><small>Expected response-time copy must be configured before publishing.</small></form></div></section></>}
